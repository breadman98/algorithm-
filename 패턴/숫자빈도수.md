```
# 백준 2577 숫자의 개수
// 각 숫자가 몇 번 나왔는지 세는 패턴
// 한자리 수로 쪼개서 그 수를 임시배열의 인덱스로 넣어서 +1씩 연산해주는 패턴
// 임시변수는 반복문 안에서 값을 할당하는 것이 아니라 증감연산을 해줄 것이기 때문에 0으로의 초기화가 필요하다.
// while(num>0){
//  n_idx = num % 10;
//  temp[n_idx] += 1;
//  num /= 10;
// }
```
